# Generate a new password pass-name of length N without any symbols
pass generate -n pass-name N

# Generate a new password to replace an existing pass-name
#   of lenght N without any symbols
pass generate -in pass-name N
